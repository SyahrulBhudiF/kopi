---
import Card from "./ui/card.astro";

export interface Props {
	method: string;
	ratio: string;
	dose: string;
	yield: string;
}
const { method, ratio, dose, yield: yieldAmount } = Astro.props;
---

<Card>
	<p class="text-rose-pine-muted">{method}</p>
	<p class="font-medium text-rose-pine-text">{ratio}</p>
	<p class="text-rose-pine-subtle">Dose {dose} → Yield {yieldAmount}</p>
</Card>
